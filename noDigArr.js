
  
        function numToDigit(n) { 
            s=n.toString();
            console.log(s);
            var str = s.split(''); 
            var arr = "[" + str + "]"; 
            console.log(arr);
        } 

        numToDigit(5247);